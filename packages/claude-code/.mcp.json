{
  "mcpServers": {
    "keepnow": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/dist/server.js"],
      "env": {
        "KEEPNOW_SESSION_DIR": "${CLAUDE_PLUGIN_DATA}/sessions",
        "KEEPNOW_PROJECT_DIR": "${CLAUDE_PROJECT_DIR}"
      }
    }
  }
}
